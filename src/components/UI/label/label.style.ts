import { stylesheet } from "typestyle";

export const style = stylesheet({
    label: {
        fontSize: '14px',
        lineHeight: '19px',
        display: 'flex',
        alignItems: 'center',
        width: '150px',
        color: '#999999'
    }
})